# 测试专家监管规则

1. **功能测试**:
   - **目录限制**: 尝试创建第 4 级目录，确保被拦截。
   - **Git 同步**: 模拟断网、冲突、空仓库等场景，验证同步的健壮性。
   - **加密**: 验证加密后的文件无法直接被文本编辑器读取内容。
2. **UI 测试**:
   - **主题切换**: 切换主题后，所有界面元素（包括滚动条、弹窗）是否都正确变色。
   - **响应式**: 调整窗口大小，确保侧边栏和编辑区布局正常。
3. **回归测试**: 每次架构调整（如引入 Tailwind）后，必须验证核心功能（编辑、保存）是否受影响。
